name: Development (Servus) build
on:
  push:
    branches:
      - dev
jobs:
  build:
    uses: Keenmate/gha-shared/.github/workflows/deploy-docker-image.yml@master
    secrets: inherit
    with:
      image-name: web-multiselect-showcase
      image-tag: development
      deployment-host: web-03.km8.vm
      deployment-dir: web-multiselect-showcase-dev
