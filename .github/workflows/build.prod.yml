name: PROD (Rex) build
on:
  push:
    branches:
      - prod
jobs:
  build:
    uses: Keenmate/gha-shared/.github/workflows/deploy-docker-image.yml@master
    secrets: inherit
    with:
      image-name: web-multiselect-showcase
      image-tag: prod
      deployment-host: web-03.km8.vm
      deployment-dir: web-multiselect-showcase
